var n = Math.random();
console.log(n);//will generate floating numbers b/w 0(inclusive) and 1(exclusive)

// code snippet to generate randomly between 1 to 100
var n = Math.random();
console.log(Math.floor(n*100));

// code snippet to generate res on dice when rolled:
var n = Math.random();
console.log(Math.floor(n*6) + 1);
